import styled from 'styled-components';

const SubMessage = styled.p.attrs({ className: 'sub-message' })`
  margin: 0;
  font-size: 9vw;
  line-height: 0.8;
  color: #fff;
  text-align: left;
`;

export default SubMessage;
