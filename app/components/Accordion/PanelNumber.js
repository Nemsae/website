import styled from 'styled-components';

const PanelNumber = styled.span.attrs({ className: 'panel__number' })`
  font-size: 4vw;
  font-family: serif;

  transition: all 400ms;
`;

export default PanelNumber;
