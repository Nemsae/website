import styled from 'styled-components';

const Wrapper = styled.div.attrs({ className: 'eyebrow__wrapper' })`
  ${'' /* position: relative; */}
  position: absolute;
  bottom: 58rem;
  right: 38%;
  transform: scale(1.5);
`;

export default Wrapper;
