import styled from 'styled-components';

const SectionMediaContainer = styled.div`
  ${'' /* max-width: 108rem; */}
  ${'' /* max-width: 48rem; */}

  position: absolute;
  left: 35rem;
  top: 50%;
  transform: translateY(-50%);
`;

export default SectionMediaContainer;
