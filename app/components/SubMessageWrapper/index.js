import styled from 'styled-components';

const SubMessageWrapper = styled.div.attrs({ className: 'sub-message__wrapper' })`
  display: table;
  max-width: 61rem;
`;

export default SubMessageWrapper;
