import styled from 'styled-components';

const ListWrapper = styled.div.attrs({ className: 'list__wrapper' })`
  display: flex;
  flex-direction: column;
  align-items: flex-start;
`;

export default ListWrapper;
