import styled from 'styled-components';

const CrumbNext = styled.span`
  margin: 0;
  padding: 0;

  color: #FEC0C1;
  ${'' /* color: #FFF;
  mix-blend-mode: difference; */}
`;

export default CrumbNext;
